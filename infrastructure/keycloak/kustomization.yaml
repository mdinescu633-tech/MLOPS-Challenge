apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: keycloak

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - keycloak-sealed-secret.yaml
  - realm-configmap.yaml
  - oauth2-proxy.yaml